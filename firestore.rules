rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomCode} {
      // Accessible for game server room synchronization
      allow read, write: if true;
    }
    match /test_rooms/{id} {
      allow read, write: if true;
    }
  }
}
